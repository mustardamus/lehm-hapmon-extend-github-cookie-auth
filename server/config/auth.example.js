// remove '.example' from filename to make it work (auth.js is not in version control)
'use strict'

module.exports = {
  // the password to encrypt the data
  password: 'TODO: have the template after hook generate a random password',

  // github application information
  githubClientId: 'register an oauth app at',
  githubClientSecret: 'https://github.com/settings/developers',

  // name of the cookie
  cookieName: 'appname-cookie'
}
